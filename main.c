#include "mercado.h" // Inclui o cabeçalho com as funções
#include <windows.h>

int main() {
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);
    
    menu(); 
    return 0;
    
}